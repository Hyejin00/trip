extends ../layout

block content
  .container
    h2.main-title 사용자 관리
    
    table.table.table-hover
      thead
        tr
          th Name
          th Email
          th Created At
          th
      tbody
        each user in users
          tr
            td
              a(href=`/managers/user/${user._id}`)= user.name
            td= user.email
            td= moment(user.createdAt).format('YYYY-MM-DD HH:mm:ss')
            td
              a.btn.btn-sm.need-confirm-btn.btn-outline-danger(href=`/managers/${user._id}?_method=DELETE`)
                | #[i.fa.fa-trash] Delete
    a.btn.btn-primary(href='/managers/user/new') #[i.fa.fa-plus] Register a new user